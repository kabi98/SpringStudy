<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.bmt.mapper.BoardMapper">

	<!-- 전체 게시물을 가져오는 SQL -->
	<select id="getLists" resultType = "com.bmt.entity.Bmt_post">
		select p.idx idx, p.title title, p.pdate pdate, u.nick nick, p.file file, p.view view 
		from bmt_post p, bmt_user u
		where p.email = u.email 
		order by pdate desc
	</select>

</mapper>